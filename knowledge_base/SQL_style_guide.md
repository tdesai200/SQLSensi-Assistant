# SQL Style Guide

Use explicit column names instead of SELECT * to improve readability and performance.
Format queries with line breaks for SELECT, FROM, WHERE, GROUP BY, and ORDER BY clauses.
Use consistent casing for SQL keywords.
Keep query logic modular and avoid overly complex statements.
